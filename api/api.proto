syntax = "proto3";

package api;

// Messages

message PingMessage {
	string greeting = 1;
}

message Request {
	int32 a = 1;
	int32 b = 2;
}

message Response {
	int32 result = 1;
}

// Services

service Add {
	rpc AddNumbers(Request) returns (Response) {}
}
